server:
  port: 8083
  servlet:
    context-path: /api

spring:
  application:
    name: charity-donation-system

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/charity_donation?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: charity-donation-system-secret-key-for-graduation-project-2024
  expiration: 10800000

file:
  upload:
    path: C:/Users/nonam/Desktop/前后端测试/big_event_back/uploads

logging:
  level:
    com.charity: debug
